---
const { block = {} } = Astro.props;

const title = block.title || '';
const text = block.text || '';
---

<section class="cms-block cms-section" data-block-type="section">
  <div class="cms-wrap">
    {title && <h2>{title}</h2>}
    {text && <p>{text}</p>}
  </div>
</section>
